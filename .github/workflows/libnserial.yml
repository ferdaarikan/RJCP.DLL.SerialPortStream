name: build libnserial for linux

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    env:
      APP_VERSION: 1.0.${{ github.run_number }}

    steps:        
    - uses: actions/checkout@v3
 
    - name: Build
      woring-directory: serialportstream/dll/serialunix
      run: build.sh 
      
